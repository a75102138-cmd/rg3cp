# Backend Nest (défaut PORT=3001) + préfixe global /api — ne pas utiliser le port de Next.js
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api

# Optionnel : UUID d’un acteur pour pré-sélectionner le chef de projet dans « Nouveau projet »
# NEXT_PUBLIC_DEFAULT_PROJECT_MANAGER_ACTOR_ID=
